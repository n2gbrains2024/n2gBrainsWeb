function Login({ setShow }) {
  return (
    <div>
      <button
        onClick={() => {
          setShow(true);
        }}
      >
        GRANC
      </button>
    </div>
  );
}

export default Login;
